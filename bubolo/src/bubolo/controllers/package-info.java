/**
 * Contains interfaces for controllers and controller factories, as well
 * as the concrete Controllers class, which is responsible for calling <code>update</code>
 * on all controllers once per game tick.
 */
package bubolo.controllers;